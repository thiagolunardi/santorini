# Project Mandates

## Engineering Standards
- **Validation:** You MUST ensure that each feature builds successfully (`dotnet build`) and all tests pass (`dotnet test`) before marking a task as complete.
- **Constant Casing:** Avoid CAPITAL_CASING for constants. Use PascalCase for C# and camelCase for TypeScript.
